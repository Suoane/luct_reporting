# Copy this file to `.env` and fill in the real values (do NOT commit your .env file)

# Postgres connection
DB_USER=postgres
DB_PASSWORD=your_postgres_password_here
DB_HOST=localhost
DB_NAME=luct_reporting
DB_PORT=5432

# JWT secret used to sign tokens
JWT_SECRET=luct-reporting-super-secret-key-2025

# Optional: other envs
# NODE_ENV=development
